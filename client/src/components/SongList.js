import React from 'react';

class SongList extends React.Component {
    state = { title: this.props.song.name }

    render() {
        return (
        const { title } = this.state;
        )
    }
}